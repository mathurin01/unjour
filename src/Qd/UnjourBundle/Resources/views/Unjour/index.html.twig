{# src/Qd/UnjourBundle/Resources/views/Unjour/index.html.twig #}
<!DOCTYPE html>
	<html>
		<head>
			<title>Bienvenue sur ma première page sur la PGM !</title>
		</head>
	<body>
		<h1>Hello {{ nom }} !</h1>

		<p>
		Le Hello World est un grand classique en programmation.
		Il signifie énormément, car cela veut dire que vous avez
		réussi à exécuter le programme pour accomplir une tâche simple
		afficher ce hello world !
		</p>
    <h2>Publicités</h2>
    <p>


        {% for p in pub %}
            {{ p.id }}<br />
            {{ p.dateParution|date('d/m/Y') }}<br />
            {{ p.path }}<br />
            {# p.journalid.id #}

        {% endfor %}

    </p>


    -----------------------------------------------

    <h2>Test pour Bdd</h2>
    <p>
        {% for u in une %}
            {{ u.setJournalid.nom }} -
            {{ u.setTagsid.id }}<br />
        {% endfor %}


        tDateparution(new \DateTime($tab[0]));
        $une[$i]->setJournalid()->getId($tab[2]);
        $une[$i]->setPath($tab[3]);
        $une[$i]->setPermalien($tab[4]);
        $une[$i]->setVignetteexport($tab[5]);
        $une[$i]->setTagsid()->getId(14);
    </p>
	</body>
</html>

